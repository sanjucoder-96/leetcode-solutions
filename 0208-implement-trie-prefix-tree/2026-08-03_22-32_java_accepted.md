# 208. Implement Trie (Prefix Tree)
  
<br>**Problem:** https://leetcode.com/problems/implement-trie-prefix-tree/<br>

**Difficulty:** Medium<br>
**Topics:** Hash Table, String, Design, Trie<br>
**Language:** java<br>
**Status:** Accepted<br>
**Submitted:** 2026-08-03 22:32 local time

**Runtime:** 33 ms (beats 62.87569999999999%)
**Memory:** 62.1 MB (beats 52.25599999999989%)


<!-- leetgit:submissionId=2092956227 codeHash=e7c1032ad578281923b5df6f9d776b85099bfa1781ab93b0c08e2504bc1415a9 notesHash=e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855 -->

## Solution

```java
 class Node {
    Node[] child;
    boolean isEnd;

    public Node() {
        this.child = new Node[26];
        this.isEnd = false;
    }



}
class Trie {

    Node root;
    public Trie() {
        root= new Node();
    }
    public void insert(String word) {
         Node temp=root;
        for(char c: word.toCharArray()){
            if(temp.child[c-'a']==null){
                temp.child[c-'a']= new Node();
            }
            temp=temp.child[c-'a'];

        }
        temp.isEnd=true;
    }
    
    public boolean search(String word) {
          Node temp=root;
        for(char c: word.toCharArray()){
            if(temp.child[c-'a']==null) return false;
            temp=temp.child[c-'a'];
        }
        return temp.isEnd;
    }
    
    public boolean startsWith(String word) {
        Node temp=root;
        for(char c : word.toCharArray()){
            if(temp.child[c-'a']==null) return false;
            temp=temp.child[c-'a'];
        }
        return true;
    }
}

/**
 * Your Trie object will be instantiated and called as such:
 * Trie obj = new Trie();
 * obj.insert(word);
 * boolean param_2 = obj.search(word);
 * boolean param_3 = obj.startsWith(prefix);
 */
```
