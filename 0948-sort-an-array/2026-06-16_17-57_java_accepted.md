# 948. Sort an Array
  
<br>**Problem:** https://leetcode.com/problems/sort-an-array/<br>

**Difficulty:** Medium<br>
**Topics:** Array, Divide and Conquer, Sorting, Heap (Priority Queue), Merge Sort, Bucket Sort, Radix Sort, Counting Sort<br>
**Language:** java<br>
**Status:** Accepted<br>
**Submitted:** 2026-06-16 17:57 local time

**Runtime:** 27 ms (beats 49.26899999999997%)
**Memory:** 86.8 MB (beats 8.382900000000111%)


<!-- leetgit:submissionId=2035148340 codeHash=fad3051437fe280af6d03e4d2a5585e9cda7cf82ba41540d727b96898f54c096 notesHash=e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855 -->

## Solution

```java
class Solution {
    public void merge(int[] arr,int low, int mid, int high){
        int[] temp= new int[high-low+1];
        int i=low,j=mid+1;
        int k=0;
        while(i<=mid && j<=high){
            if(arr[i]<=arr[j]){
                temp[k++]=arr[i++];
            }
            else
            temp[k++]=arr[j++];
        }
        while(i<=mid)
        temp[k++]=arr[i++];

        while(j<=high)
        temp[k++]=arr[j++];

        for(int l=low;l<=high;l++){
            arr[l]=temp[l-low];
        }
        return;
    }
    public void mergeSort(int[] arr,int low,int high){
        if(low>=high) return;
        int mid= low + (high-low)/2 ;
        mergeSort(arr,low,mid);
        mergeSort(arr,mid+1,high);
        merge(arr,low,mid,high);
    }
    public int[] sortArray(int[] nums) {
        int n=nums.length;
        mergeSort(nums,0,n-1);
        return nums;
    }
}
```
