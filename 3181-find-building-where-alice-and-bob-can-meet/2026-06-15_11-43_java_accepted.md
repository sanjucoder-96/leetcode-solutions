# 3181. Find Building Where Alice and Bob Can Meet
  
<br>**Problem:** https://leetcode.com/problems/find-building-where-alice-and-bob-can-meet/<br>

**Difficulty:** Hard<br>
**Topics:** Array, Binary Search, Stack, Binary Indexed Tree, Segment Tree, Heap (Priority Queue), Monotonic Stack<br>
**Language:** java<br>
**Status:** Accepted<br>
**Submitted:** 2026-06-15 11:43 local time

**Runtime:** 60 ms (beats 42.1653%)
**Memory:** 198 MB (beats 33.618300000000055%)


<!-- leetgit:submissionId=2033536634 codeHash=1179c9a4f6a911cfdbe24a15396adea57cabf7c361df034f326ce16966de1e73 notesHash=e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855 -->

## Solution

```java
class Solution {
    public int[] leftmostBuildingQueries(int[] heights, int[][] queries) {
        
        int[] res = new int[queries.length];
        Arrays.fill(res,-1);
        int n=heights.length;
        List<List<int[]>> def = new ArrayList<>();
        for(int i=0;i<n;i++){
            def.add(new ArrayList<>());
        }
        for(int i=0;i<queries.length;i++){
            int x=queries[i][0],y=queries[i][1];
            if(x>y){
                int temp=x;
                x=y;
                y=temp;
            }
            if(x==y || heights[x]<heights[y]){
                res[i]=y;
            }
            else
            def.get(y).add(new int[]{heights[x],i});
        }
        PriorityQueue<int[]> pq = new PriorityQueue(Comparator.comparing((int[] a)->a[0]));
        for(int i=0;i<n;i++){
            for(int[] query: def.get(i)) pq.add(query);

            while(!pq.isEmpty() && pq.peek()[0]<heights[i])
            res[pq.poll()[1]]=i;
        }
        return res;
    }
}
```
