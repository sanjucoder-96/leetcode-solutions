# 513. Find Bottom Left Tree Value
  
<br>**Problem:** https://leetcode.com/problems/find-bottom-left-tree-value/<br>

**Difficulty:** Medium<br>
**Topics:** Tree, Depth-First Search, Breadth-First Search, Binary Tree<br>
**Language:** java<br>
**Status:** Accepted<br>
**Submitted:** 2026-07-10 15:21 local time

**Runtime:** 2 ms (beats 72.566%)
**Memory:** 47.3 MB (beats 5.869199999999978%)


<!-- leetgit:submissionId=2062708343 codeHash=708e883570119cdf11a135f173a3f5714ab42c012c519b8bab6ce4c107b18660 notesHash=e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855 -->

## Solution

```java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    public int levels(TreeNode root){
        if(root==null) return 0;
        return 1+Math.max(levels(root.left),levels(root.right));
            }
    public void dfs(TreeNode root,Map<Integer,Integer> mp,int level){
        if(root==null) return ;
        if(!mp.containsKey(level)){
            mp.put(level,root.val);
        }
        dfs(root.left,mp,level+1);
        dfs(root.right,mp,level+1);

    }
    public int findBottomLeftValue(TreeNode root) {
      if(root.left==null && root.right==null) return root.val;
        int l=levels(root);
        Map<Integer,Integer> mp= new HashMap<>();

        dfs(root,mp,0);
        return mp.get(l-1);

    }
}
```
