# 107. Binary Tree Level Order Traversal II
  
<br>**Problem:** https://leetcode.com/problems/binary-tree-level-order-traversal-ii/<br>

**Difficulty:** Medium<br>
**Topics:** Tree, Breadth-First Search, Binary Tree<br>
**Language:** java<br>
**Status:** Accepted<br>
**Submitted:** 2026-06-19 09:41 local time

**Runtime:** 1 ms (beats 96.3999%)
**Memory:** 44.3 MB (beats 75.45689999999999%)


<!-- leetgit:submissionId=2038199834 codeHash=a524c659ae36e10d823b4170f6ddc1e5b80d53a93d3fe701c7aa7a9b2979eae9 notesHash=e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855 -->

## Solution

```java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    public List<List<Integer>> levelOrderBottom(TreeNode root) {
        Deque<TreeNode> dq =new ArrayDeque<>();
        List<List<Integer>> res  = new ArrayList<>();
        if(root!=null) dq.add(root);
        while(!dq.isEmpty()){
            int s=dq.size();
            List<Integer> a = new ArrayList<>();
            while(s-- > 0){
                TreeNode r = dq.poll();
                a.add(r.val);
                if(r.left!=null) dq.add(r.left);
                if(r.right!=null) dq.add(r.right);
            }
            res.add(a);
        }
        Collections.reverse(res);
        return res;
    }
}
```
