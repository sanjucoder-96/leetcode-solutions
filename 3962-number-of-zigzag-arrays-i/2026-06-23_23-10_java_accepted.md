# 3962. Number of ZigZag Arrays I
  
<br>**Problem:** https://leetcode.com/problems/number-of-zigzag-arrays-i/<br>

**Difficulty:** Hard<br>
**Topics:** Dynamic Programming, Prefix Sum<br>
**Language:** java<br>
**Status:** Accepted<br>
**Submitted:** 2026-06-23 23:10 local time

**Runtime:** 200 ms (beats 92.5%)
**Memory:** 43.9 MB (beats 95%)


<!-- leetgit:submissionId=2043717234 codeHash=8721f0759d776a9cbba00e707de41be21c10c5c80545c99a436840a0a659a19c notesHash=e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855 -->

## Solution

```java
class Solution {
    private static final int MOD = 1000000007;

    public int zigZagArrays(int n, int l, int r) {
        int m = r - l + 1;
        int[] dp = new int[m];
        Arrays.fill(dp, 1);

        for (int i = 2; i <= n; i++) {
            int sum = 0;
            if ((i & 1) == 0)
                for (int j = 0; j < m; j++) {
                    int t = dp[j];
                    dp[j] = sum;
                    sum = (sum + t) % MOD;
                }
            else
                for (int j = m - 1; j >= 0; j--) {
                    int t = dp[j];
                    dp[j] = sum;
                    sum = (sum + t) % MOD;
                }
        }

        int res = 0;
        for (int j = 0; j < m; j++)
            res = (res + dp[j]) % MOD;

        return (res << 1) % MOD;
    }
}
```
